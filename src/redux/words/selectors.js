export const selectCategories = state => state.words.categories;

export const selectFilters = state => state.words.filters;

export const selectAllWords = state => state.words.items;

export const selectStats = state => state.words.stats;

export const selectWordsLoading = state => state.words.loading;
